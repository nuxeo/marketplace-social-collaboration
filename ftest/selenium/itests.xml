<project name="nuxeo-social-collaboration-tests"
         xmlns:nx="urn:nuxeo-build"
         xmlns:artifact="urn:nuxeo-artifact">
  <taskdef resource="org/nuxeo/build/antlib.xml" uri="urn:nuxeo-build" />
  <taskdef resource="org/nuxeo/build/artifact/antlib.xml"
           uri="urn:nuxeo-artifact" />
  <taskdef resource="net/sf/antcontrib/antlib.xml" />

  <property name="wizard.preset" value="nuxeo-cap" />

  <property name="suites" value="suite" />

  <property name="out.dir" value="${maven.project.build.directory}" />
  <unzip dest="${out.dir}/" overwrite="false">
    <artifact:resolveFile key="org.nuxeo:nuxeo-ftest::zip" />
  </unzip>
  <import file="${out.dir}/nuxeo-ftest.xml" />

  <property name="mp.install"
            value="file:${out.dir}/marketplace-${maven.project.version}.zip" />
  <target name="prepare-environment"
          depends="_init,prepare-db,prepare-tomcat,prepare-jboss">
    <copy todir="${out.dir}">
      <artifact:resolveFile key="org.nuxeo.marketplace.social.collaboration:marketplace::zip" />
    </copy>
    <antcall target="set-conf">
      <param name="name" value="org.nuxeo.connect.url" />
      <param name="value" value="https://connect-test.nuxeo.com/nuxeo/site/" />
    </antcall>
  </target>

  <target name="run-selenium-tests" depends="run-selenium" />
  <target name="add-smtp-conf">
    <antcall target="set-conf">
      <param name="name" value="mail.transport.host" />
      <param name="value" value="merguez.in.nuxeo.com" />
    </antcall>
    <antcall target="set-conf">
      <param name="name" value="mail.transport.port" />
      <param name="value" value="2500" />
    </antcall>
    <antcall target="set-conf">
      <param name="name" value="mail.from" />
      <param name="value" value="sc@test.com" />
    </antcall>
  </target>

</project>
